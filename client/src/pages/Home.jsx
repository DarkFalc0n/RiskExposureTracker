import React from 'react';
import { cn } from '@/lib/utils';

const Home = ({ className }) => {
  return <div className={cn(className, "flex flex-col items-center justify-center grow")}>Home</div>;
};

export default Home;
